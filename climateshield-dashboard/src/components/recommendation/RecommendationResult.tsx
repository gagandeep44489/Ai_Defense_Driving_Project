import { Card, CardContent, Chip, Grid, LinearProgress, List, ListItem, ListItemText, Typography } from '@mui/material';
import type { RecommendationResponse } from '../../types/recommendation';
import { MetricCard } from '../common/MetricCard';
export function RecommendationResult({ result }: { result: RecommendationResponse }) { return <Grid container spacing={2} sx={{ mt: 2 }}><Grid item xs={12} md={3}><MetricCard title="Risk Score" value={result.risk_score} helper={result.severity} /></Grid><Grid item xs={12} md={3}><MetricCard title="Priority" value={result.priority} /></Grid><Grid item xs={12} md={6}><Card><CardContent><Typography>Preparedness Score: {result.preparedness_score}</Typography><LinearProgress variant="determinate" value={result.preparedness_score} sx={{ my: 2 }} /><Typography variant="body2">{result.preparedness_explanation}</Typography></CardContent></Card></Grid><Grid item xs={12} md={6}><Card><CardContent><Typography variant="h6">AI Recommendations</Typography><List>{result.recommendations.map((item) => <ListItem key={item}><ListItemText primary={item} /></ListItem>)}</List></CardContent></Card></Grid><Grid item xs={12} md={6}><Card><CardContent><Typography variant="h6">Long-term Actions</Typography><List>{result.long_term_actions.map((item) => <ListItem key={item}><ListItemText primary={item} /></ListItem>)}</List>{result.categories.map((category) => <Chip key={category} label={category} sx={{ mr: 1, mb: 1 }} />)}</CardContent></Card></Grid></Grid>; }
